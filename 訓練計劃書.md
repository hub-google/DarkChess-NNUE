世界頂級暗棋 AI 訓練與驗證專案企劃書

一、 專案目標與挑戰

本專案旨在打造一款世界最強、且具備自我進化能力的暗棋（Banqi）人工智慧。

硬體限制：無 GPU 環境，必須完全依賴 CPU 運算。

領域挑戰：暗棋屬於隨機型不完全資訊遊戲，先手翻牌運氣成分極重，且中盤存在大量未知機率節點。

解決策略：採用位元棋盤（Bitboard）極致優化 CPU 效能、NNUE 輕量化神經網路作為評估函數，並導入「強制同源鏡像對弈」來精準驗證棋力成長。

二、 核心運算架構設計 (純 CPU 最佳化)

為突破無 GPU 的限制，引擎底層必須極度輕量、高效。

盤面表示：位元棋盤 (Bitboard)

將 32 格的暗棋盤面映射為 64-bit 的整數陣列（例如：紅帥位置、未翻開位置等）。

透過位元運算（Bitwise operations）取代傳統陣列迴圈，將「合法步生成」與「盤面狀態更新」的速度推向 CPU 效能極限。

搜尋演算法：機率型 Alpha-Beta 剪枝 (Star 演算法族)

捨棄 MCTS（蒙地卡羅樹搜尋），改採專為機率遊戲設計的 Star1 / Star2 演算法。

在搜尋樹中加入「機率節點（Chance Nodes）」，計算未翻開棋子的期望值，並利用數學界線進行高效剪枝。

評估函數：NNUE (Efficiently Updatable Neural Network)

使用極小型的神經網路架構。當棋盤發生單一變動時（移動或翻開），僅需更新受影響的網路節點，完美契合 CPU 的 SIMD (AVX2/AVX-512) 指令集，實現每秒百萬級別的推論速度。

終局決策：殘局庫 (Endgame Tablebases)

預先計算並儲存 4~6 子的所有絕對勝負結果。搜尋樹觸碰到殘局庫條件時直接回傳分數，節省寶貴的運算深度。

三、 棋力驗證機制 (降低隨機變異數的核心)

為了解決「先手運氣」導致無法確認 AI 是否真實變強的痛點，本專案採用「擴充版橋牌複式賽 (Duplicate Match)」機制。

隨機底盤生成庫

在每次驗證階段，系統隨機產生一批（如 1,000 個）合法的 32 子隱藏底盤，並固定隨機亂數種子（Seed），確保盤面可重現。

強制開局點位分配

為這批底盤隨機分配一個「強制首步翻牌座標」（例如：(3,4) 或 (1,1)），確保開局多樣性，防止 AI 對特定開局（如 2,2）產生過度擬合。

鏡像交叉對戰 (AB Test)
對於每一個測試盤面（底盤 X，強制翻牌點 Y）：

Game A：新模型 (先手) vs 舊模型 (後手) — 系統強制新模型翻開 Y 點。

Game B：舊模型 (先手) vs 新模型 (後手) — 系統強制舊模型翻開 Y 點。

結果結算：雙方在完全相同的初始運氣、相同的執色下交手。比較雙方在 Game A 與 Game B 的總積分。

統計檢定 (SPRT)

導入序貫機率比檢定（Sequential Probability Ratio Test）。不需要打完固定的局數，只要累積的對局勝率在統計學上達到顯著水準，即可提前判定新模型是否成功超越舊模型。

四、 持續進化迴圈 (Continuous Evolution Loop)

系統將自動執行以下無限迴圈，實現 AI 棋力的持續成長：

Phase 1: 數據生成 (Self-Play)
目前的「冠軍模型」與自己進行對戰（可加入適度的隨機雜訊確保棋譜多樣性），記錄盤面狀態與最終勝負結果。

Phase 2: 模型訓練 (Training)
在 CPU 上使用 Phase 1 產生的新棋譜，透過梯度下降法更新 NNUE 的權重參數，產生「挑戰者模型」。

Phase 3: 鏡像驗證 (Validation)
啟動「三、棋力驗證機制」，讓挑戰者模型與冠軍模型進行 SPRT 鏡像對決。

Phase 4: 晉升 (Promotion)
若挑戰者在 SPRT 檢定中獲勝，則取代冠軍模型，系統返回 Phase 1 繼續產生更高質量的棋譜。

五、 開發時程建議

第一階段 (基礎建設)：完成 Bitboard 盤面表示、遊戲規則邏輯、隨機底盤生成器。

第二階段 (演算法與驗證)：實作 Star 搜尋演算法，並架設「鏡像交叉對戰」的驗證腳本與 SPRT 檢定系統。

第三階段 (NNUE 導入)：整合 NNUE 推論引擎，撰寫自我對弈 (Self-play) 數據收集程式。

第四階段 (自動化與殘局)：串接訓練與驗證迴圈，加入殘局庫，讓 AI 開始全自動掛機進化。