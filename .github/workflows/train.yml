name: Train and validate NNUE

on:
  schedule:
    # 19:00 UTC is 03:00 the following day in Asia/Taipei (UTC+8).
    - cron: "0 19 * * *"
  workflow_dispatch:

concurrency:
  # Serialize training runs, but do not block self-play uploads. Consolidation
  # deletes only the cutoff-bounded snapshot that it successfully verifies.
  group: nnue-training
  cancel-in-progress: false

jobs:
  train-and-validate:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Python tests
        run: python -m unittest discover -s test -p "test_*.py" -v

      - name: Determine replay-buffer snapshot cutoff
        shell: bash
        run: |
          NOW_EPOCH=$(date -u +%s)
          if [ "${{ github.event_name }}" = "schedule" ]; then
            # Scheduled training represents 19:00 UTC (03:00 Asia/Taipei).
            CUTOFF_EPOCH=$(date -u -d "$(date -u +%F) 19:00:00" +%s)
            if [ "$CUTOFF_EPOCH" -gt "$NOW_EPOCH" ]; then
              CUTOFF_EPOCH=$((CUTOFF_EPOCH - 86400))
            fi
          else
            # A manual run snapshots everything fully uploaded when its job starts.
            CUTOFF_EPOCH=$NOW_EPOCH
          fi
          echo "CONSOLIDATION_CUTOFF_EPOCH=$CUTOFF_EPOCH" >> "$GITHUB_ENV"
          echo "Snapshot cutoff: $(date -u -d "@$CUTOFF_EPOCH" --iso-8601=seconds)"

      - name: Consolidate replay buffer
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: python -u src/training/consolidate_buffer.py

      - name: Download replay buffer
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          mkdir -p datasets
          python -c "from huggingface_hub import hf_hub_download; import shutil; p=hf_hub_download(repo_id='hub-google/DarkChess-NNUE-Data', repo_type='dataset', filename='replay_buffer.jsonl.gz'); shutil.copy(p, 'datasets/replay_buffer.jsonl.gz')"

      - name: Train challenger from champion
        env:
          BATCH_SIZE: "1024"
          TRAINING_EPOCHS: "3"
          LEARNING_RATE: "0.0001"
          MAX_POSITIONS_PER_GAME: "4"
          MAX_TRAINING_SAMPLES: "2000000"
        run: python -u src/training/train.py

      - name: Validate challenger
        env:
          SPRT_PAIRS: "200"
        run: |
          if [ ! -f models/champion.nnue ]; then
            echo "No existing champion; allowing the first validated bootstrap model."
            echo "PASSED=true" >> "$GITHUB_ENV"
          else
            python -u src/training/sprt_validation.py \
              --champion models/champion.nnue \
              --challenger models/challenger.nnue \
              --pairs "$SPRT_PAIRS" \
              --depth 1 \
              --elo0 0 \
              --elo1 15
          fi

      - name: Promote validated challenger
        if: env.PASSED == 'true'
        run: |
          mv models/challenger.nnue models/champion.nnue
          python src/training/export_nnue.py \
            --input models/champion.nnue \
            --output-dir frontend/public/models
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add models/champion.nnue frontend/public/models/
          if ! git diff --staged --quiet; then
            git commit -m "chore: promote validated NNUE champion"
            git push
          fi

      - name: Reject unproven challenger
        if: env.PASSED != 'true'
        run: |
          echo "Challenger did not cross the SPRT H1 boundary; champion is unchanged."
          exit 1
